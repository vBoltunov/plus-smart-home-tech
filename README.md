# Smart Home Platform

Это микросервисная платформа для управления умным домом с бэкендом интернет-магазина умных устройств. Платформа обеспечивает сбор и анализ телеметрии от датчиков в реальном времени, автоматический запуск пользовательских сценариев автоматизации, а также предоставляет серверную часть (backend-only) для электронной коммерции - без фронтенда.

## Основные модули

### Модуль телеметрии (Telemetry)

Система реального времени для обработки данных от датчиков и актуаторов умного дома.

**Поток данных:**

- Данные от пользовательских хабов поступают в сервис **Hub Router**, который нормализует их и передаёт дальше.
- **Collector** получает сообщения через gRPC, сериализует в Avro и помещает в топики Apache Kafka. Также обрабатывает события изменения конфигурации (добавление/удаление устройств и сценариев).
- **Aggregator** потребляет данные датчиков из Kafka, группирует их по хабам и формирует актуальный «снимок» состояния всех устройств в доме. Результат записывается в отдельный топик Kafka.
- **Analyzer** читает агрегированные снимки, сохраняет их в БД при необходимости и проверяет выполнение условий пользовательских сценариев. При совпадении отправляет команды на исполнение через **Hub Router**, который передаёт их соответствующему хабу.

Таким образом, система поддерживает реактивное управление устройствами на основе текущего состояния.

### Бэкенд интернет-магазина (Commerce) - backend-only

Серверная часть электронной коммерции для продажи умных устройств. Фронтенд не реализован - все взаимодействие происходит через REST/gRPC API.

**Ключевые сервисы и возможности:**

- **Shopping Store**  
  Управление каталогом: создание, обновление, удаление товаров, получение списка с пагинацией, детальная информация о продукте.

- **Shopping Cart**  
  Управление корзиной авторизованного пользователя: добавление/удаление позиций, изменение количества, просмотр содержимого, деактивация корзины.

- **Warehouse**  
  Управление складом: приёмка товаров, учёт остатков, проверка наличия по корзине, сборка заказа, передача в доставку, обработка возвратов.

- **Order**  
  Жизненный цикл заказа: создание, расчёт стоимости (включая доставку), оплата, сборка, отгрузка, обработка ошибок (оплата, сборка, доставка), возвраты, просмотр истории заказов клиента.

- **Payment**  
  Эмуляция платёжного шлюза.

- **Delivery**  
  Эмуляция сервиса доставки: расчёт стоимости, приёмка заказа, сохранение данных о доставке.

### Инфраструктурный модуль (Infra)

Общие компоненты микросервисной архитектуры:
- API Gateway для маршрутизации
- Config Server для централизованного управления конфигурациями
- Service Discovery (Eureka)
- Межсервисное взаимодействие через gRPC и OpenFeign

## Архитектура

Многомодульный проект. В модуле телеметрии преобладает асинхронное взаимодействие через Apache Kafka. В модуле commerce - преимущественно синхронное через gRPC/OpenFeign. Все сервисы регистрируются в Service Discovery и доступны через API Gateway.

## Технологический стек

- Java 21
- Spring Boot 3.3.2
- Spring Cloud 2023.0.3
- Apache Kafka (clients 3.6.1)
- gRPC 1.65.1
- Apache Avro 1.11.3
- PostgreSQL 42.7.4
- Protocol Buffers 3.25.3
- SpringDoc OpenAPI 2.6.0
- Project Lombok
- Maven
- Docker

## Запуск проекта

1. Склонируйте репозиторий.
2. Установите и запустите Docker Desktop
3. В консоли перейдите в папку проекта и выполните команду 

Платформа готова к интеграции с вашим фронтендом и дальнейшему развитию умного дома! 🏠
